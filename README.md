This project uses the Random Forest classification algorithm to recognize handwritten digits from the Scikit-learn Digits dataset. 
The model was trained on image pixel features, evaluated using accuracy and confusion matrix.
